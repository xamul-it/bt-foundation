Env files in this directory are local runtime configuration.

Tracked files are only `*.example` templates. Copy the needed template to the
matching non-example filename and fill secrets locally.

Do not commit real Alpaca keys, broker keys, webhook secrets, database
passwords, or `live.key`.
